buildscript {
    ext.publish_description = 'Contains classes to manipulate a release plan.'
}

dependencies {
    compile prefixedProject('api-jvm'), withoutKotlin

    testCompile prefixedProject('maven').sourceSets.test.output
}
