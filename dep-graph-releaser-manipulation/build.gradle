project.description = 'Contains classes to manipulate a release plan.'

dependencies {
    compile prefixedProject('api-jvm'), excludeKotlin
    compile "ch.tutteli.niok:niok:$niok_version", excludeKotlin

    testCompile prefixedProject('maven').sourceSets.test.output
}
